generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Procedure {
  id                Int    @id @default(autoincrement())
  name              String
  modality          String
  clinics           String
  bookingCategories String
  notes             String
}
